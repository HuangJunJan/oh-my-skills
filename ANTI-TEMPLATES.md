# ANTI-TEMPLATES — 我们故意不预制什么

每次决定 **不** 往 `templates/` 里加某个东西，把决策记在这里。

## 禁止预制清单

| 内容 | 为什么不过模板 | 首次记录 |
|------|---------------|---------|
| 默认 lint/format 规则 | 语言特定，下游必须自己写 | 初始 |
| 默认 commit message 格式 | 团队偏好不同 | 初始 |
| 预填的常见坑点 | 坑是真金白银的调试换来的，不是想出来的 | 初始 |
| 默认目录结构 (src/test/docs) | 每个框架约定不同 | 初始 |
| 具体 subagent spec 样本 | 内容层，下游必须自己写 | 初始 |
| 默认包管理器和版本 | 项目技术栈不同 | 初始 |
| 具体 CI/CD 配置 | 平台不同（GitHub Actions / GitLab CI / Jenkins） | 初始 |

## Deprecation Log

| 日期 | 废弃内容 | 原因 | 替代方案 |
|------|---------|------|---------|
| — | — | — | — |

## Homogeneity Drift Log

| 日期 | 检查项目 | 发现 | 修复 |
|------|---------|------|------|
| — | — | — | — |