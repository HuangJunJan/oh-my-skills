# 指令层级

## 优先级

从高到低：

```
用户当次明确要求
  > 更近层级项目规则（如 AGENTS.md / CLAUDE.md）
    > 本 oms-meta
      > 其它 oms-* skills
        > 工具默认行为
```

冲突时取更高层级；无法同时满足时，停止并说明冲突点。
检验：改动后自问"如果用户明确说了相反的话，这条规则会让路吗？"——不会就让路。
