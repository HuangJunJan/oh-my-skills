# 已知陷阱

本文件记录来自真实失败的教训，每条必须通过 2/3 录入标准。
格式：标题、触发条件、根因、避免方式。禁止凭空编造。

---

<!-- 初始为空，从真实失败中捕获。格式示例：

## 平台不兼容：PowerShell 变量语法

- **触发**：在 Windows 项目里用 `$VAR` 而非 `$env:VAR`
- **根因**：PowerShell 不认 POSIX 风格的 `$VAR`
- **代价**：命令静默失败，Agent 误判为代码缺陷
- **避免**：每个命令写完后对照 `rules/platform-baseline.md` 的表格

-->
