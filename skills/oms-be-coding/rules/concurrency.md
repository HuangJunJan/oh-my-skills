# 并发与韧性

## 共享状态
共享状态必须有明确锁、事务、队列、actor、channel 或无共享设计。

## 异步任务
- 异步任务必须有超时、重试策略、幂等保护和失败可见性。
- 禁止裸 fire-and-forget。
检验：这个异步任务失败了有人知道吗？— 没有就加失败可见性。

## 外部调用
- 外部调用配置超时、重试、限流或熔断。
- 重试必须避免放大故障（exponential backoff + jitter + 最大重试次数）。

## 资源池
资源池（DB/HTTP/thread/goroutine）容量和超时显式配置，生产路径不依赖危险默认值。
